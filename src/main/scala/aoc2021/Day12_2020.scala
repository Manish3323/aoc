package com.example.aoc

@main def Day12_2020 = {
//  val strings = IO.readLines("12_2020.txt")
//  val twoDimensionalArray = strings.map(x=>x.toCharArray)
//  class Grid(grid: Array[Array[Char]]) {
//
//    def countL(grid: Array[Array[Char]]) : Int = ???
//
//    def applyRules(char: Char, neighbours: Array[Char]): Char = {
//      if(char == 'L' && neighbours.count{ x => x == 'L'} == 0) '#'
//      else if(char == '#' && neighbours.count{ x => x == '#'} >= 4) 'L'
//      else char
//    }
//    def nextLife(previousGrid: Array[Array[Char]]): Grid = {
////      val nGrid = nextLife(previousGrid)
////      if(previousGrid == nGrid)
////        print(countL(grid))
////        return nGrid
////      else
////        return for {
////          r <- nGrid.grid
////        }
////    }
//  }
////  def applyRules(x: Int, y: Int, grid: Array[Array[Char]]): Char = {
////    if(grid(x)(y) == '.')
////      return '.'
////    else if (grid(x)(y) == 'L') return 'L'
////    else if (grid(x)(y) == '#') return 'L'
////  }

}

